name: ci/gitlab-ci
version: 0.0.13
description: Reviews GitLab CI for privileged runners, secret leakage, mutable images, release safety, and unpinned includes.

triggers:
  manual: true
  files_changed:
    - ".gitlab-ci.yml"
    - ".gitlab-ci.yaml"
    - ".gitlab/ci/*.yml"
    - ".gitlab/ci/*.yaml"
    - ".gitlab/ci/**/*.yml"
    - ".gitlab/ci/**/*.yaml"
    - "**/*.yml"
    - "**/*.yaml"

detection:
  files:
    - ".gitlab-ci.yml"
    - ".gitlab-ci.yaml"
    - ".gitlab/ci/*.yml"
    - ".gitlab/ci/*.yaml"
    - ".gitlab/ci/**/*.yml"
    - ".gitlab/ci/**/*.yaml"
    - "**/*.yml"
    - "**/*.yaml"

runtime:
  name: node
  version: "22"
  command:
    - dist/index.js

permissions:
  enforcement: advisory
  filesystem:
    read:
      - .
    write:
      - .adversary/results
  network: false
  environment:
    allow: []

findings:
  format: adversary.review.v1
